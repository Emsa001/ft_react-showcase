import React, { useEffect, useRef, setTitle } from "react";

export default function Profile() {
    return (
        <div className="min-h-screen flex items-center justify-center bg-gray-900 px-4">
            Memes will be here, I promise
        </div>
    );
}
